build-idx:
  generated-by: csolution 2.11.0
  csolution: image-only.csolution.yml
  cbuild-run: out/image-only+AVH.cbuild-run.yml
  tmpdir: tmp
  image-only: true
  cbuilds:
    - cbuild: out/image-only/AVH/image-only+AVH.cbuild.yml
      configuration: +AVH
  executes:
    - execute: Test
      run: ${CMAKE_COMMAND} -E echo "Test"
